{
  "name": "explainer-engine",
  "private": true,
  "version": "0.1.0",
  "description": "Natural-language question -> narrated, animated explainer video",
  "workspaces": [
    "packages/spec",
    "packages/planner",
    "packages/compositor",
    "packages/api"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
